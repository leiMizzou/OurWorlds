# Godot 4 导入缓存 / 临时文件
.godot/
*.tmp

# 开发期截图（测试产物，可由 tests/shot_*.gd 重新生成）
_shot_*.png
_shot_*.png.import

# 导出 / 打包产物
build/
*.pck
*.zip

# 系统文件
.DS_Store
Thumbs.db

# Node / MCP 服务器（构建产物与本地配置，不入库）
agent-bridge-mcp/node_modules/
agent-bridge-mcp/dist/
agent-bridge-mcp/config/
*.tsbuildinfo

# 每用户的 agent 运行期记忆
agent_memory.json

# Python 缓存
__pycache__/
*.pyc
